# MathModel-Pipeline-Agent

国赛数学建模端到端流水线：四 Agent（协调者→建模手→代码手→论文手）协作完成从题目到论文的全流程，配套质量门与人工审批。

## Language

### 质量门体系

**质量门（Quality Gate）**:
流水线固定位置上的产物验收关卡，产出三档判定之一。门只判定、不修改产物。
_Avoid_: 检查点（这个词留给人工审批环节）、校验器

**门判定三档（PASS / MINOR / MATERIAL）**:
PASS 放行；MINOR 携带 ≤3 条小问题放行并提示人工；MATERIAL 阻断并触发修复回路。

**修复回路（Repair Loop）**:
MATERIAL 判定后，带修订路线图退回产出该产物的 Agent 做补丁式修订（禁止全量重写）的自动循环，硬上限 3 轮；耗尽后升级为人工显式决策。

**修订路线图（Revision Roadmap）**:
修复回路的载体，每条修订项分离五个字段：严重度 / 义务级别 / 修复范围 / 验收判据 / 目标位置。由门产出，不可被修复方篡改。
_Avoid_: 评审意见（泛指，含人工意见）、整改清单

**义务级别（Obligation Class）**:
修订项的必须程度：must_fix（不清零不放行）/ should_fix（解决率 ≥80% 可放行）/ consider（建议）。

**严重度（Severity）**:
单条问题对核心结论的决策影响：Critical（单条不修即否决）/ Major（实质削弱但核心存活）/ Minor（不影响核心）。

**终审（Final Review）**:
论文组装后的一次 LLM 评审，用分类判据与机械规则裁决（任一 must_fix 恶化即退回；must_fix 100% + should_fix ≥80% 清零即通过），不使用数字总分。

### 人工审批

**人工检查点（Human Checkpoint）**:
流水线暂停并等待人类决策的位置。进入暂停态后无响应则永久等待，绝不自动放行。
_Avoid_: 审批门、HIL 点

**审批三分支（Approve / Revise / Reject）**:
人工检查点上的三个动作：批准放行；带意见返工（人工意见注入 Agent 对话历史做定向修订后回到同一检查点）；否决中止（材料保留）。人工返工不设轮次上限。

**人工意见（Human Feedback）**:
人类在审批时提交的自由文本指导，注入目标 Agent 的对话历史作为修订依据，全部记入审计。

**数据完备性门（G1）**:
问题拆解后核对题面所需附件与工作目录实存文件的门，缺失即任务硬中止（唯一一票否决的门——数据缺失非 Agent 可修复）。

**数值重算（Recompute）**:
G4 内的确定性算术验证子层：均值可达性、p 值重算、自由度与样本量一致性。判定四态：consistent / mismatch / not_computable(reason) / not_applicable。
_Avoid_: 数值校验、数字核对

**种子缺陷回归（Seeded-Defect Regression）**:
质量门自身的回归测试：合成带唯一锚文本的缺陷工件 + 干净对照稿，验证门能命中锚点且对照零误报。改门前先测基线，看 ≥2 次重复的均值。
_Avoid_: 门测试（泛）、评测集（泛）
